[project]
name = "homeassistant-bmr"
version = "2.1.0"
description = "BMR HC64 integration for Home Assistant"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "homeassistant>=2026.2.3",
    "isal>=1.8.0",
    "zlib-ng>=1.0.0",
]
